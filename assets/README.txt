assets/ — the HAND-AUTHORED files of the published site (2026-08-29).
Every bin/build.sh run CLEARS its scope's docs/ tree and re-seeds these
files into it (style.css / report.js / slotchart.js / file-search.js ->
docs/assets/, help/ -> docs/help/). docs/ is pure build output again:
EDIT HERE, never in docs/ — a build overwrites the docs copies.
(.nojekyll and topbar-data.js are generated by ensure_assets, not seeded.)
