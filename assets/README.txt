assets/ — the HAND-AUTHORED files of the published site (2026-08-29).
Every bin/build.sh run CLEARS docs/ wholesale and re-seeds these files
into it (style.css / report.js / slotchart.js / file-search.js ->
docs/assets/, help/ -> docs/help/). docs/ is pure build output:
EDIT HERE, never in docs/ — a build overwrites the docs copies.
(.nojekyll and topbar-data.js are generated by ensure_assets, not seeded;
the help pages' baked top bar is re-stamped by apply_help_chrome at every
build, so the copies here carry whatever bar the last build produced.)
