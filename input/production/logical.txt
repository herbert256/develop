# input/<env>/logical.txt — FIXED FlowID -> Logical transforms for the Logical
# entity derivation (bin/flow-manager.sh), shared by both environments. A
# pinned Logical that is not a three-part D_A_P name also opts its family
# OUT of the partner/domain/application derivation (it contributes nothing). Two whitespace-separated columns, # comments and blank
# lines ignored:
#
#     <FlowID> <Logical>
#
# A listed FlowID takes its given Logical VERBATIM — no grouping and no
# 3-part reshape; only unlisted FlowIDs go through the derivation. Use it
# where the algorithm's mechanical shape is wrong for a flow.
#
# A mapping also applies to the GROUP NAME a fold lands on, so one line
# pins a whole folded family's final form.
#
# SAMPLE ESTATE: the 2-token WA_VDN would mechanically double into
# WA_WA_VDN — the fixed form names its middle part properly; the monitor
# family groups onto INFRA_MONITOR_UC (separators normalize to "_" for
# grouping) and the pin keeps its native dashed spelling.
WA_VDN WA_CLEARING_VDN
INFRA_MONITOR_UC INFRA-MONITOR-UC
