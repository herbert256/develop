input/production/ — SAMPLE data for the production environment, generated by
bin/sample/generate.sh. Synthetic end to end: fake partners, TEST-NET
addresses, .example hosts. See input/README.txt and bin/sample/. The nine
policy files here (blacklist, skip, rename, logical*, BL, partner-aliases)
are this environment's own — the same template seeds both sample environments.
